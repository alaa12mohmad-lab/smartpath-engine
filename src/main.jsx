import React from 'react'
import ReactDOM from 'react-dom/client'
import SmartPathEngine from './SmartPathEngine'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SmartPathEngine />
  </React.StrictMode>
)
